Merchant;
Galaxy;
Calculator - API;
Accepts;
input;
of;
different;
galaxy;
currency;
and;
computes;
the;
value;
based;
on;
how;
it;
was;
defined.
    See[GalaxyStory.md](https, Installation, Pre - requisites
    - [Node](https, //nodejs.org) >= 4
    -[Mocha](https, (_a = [""], _a.raw = [""], (_b = [" sh\n  $ git clone https://github.com/dennisjade/merchant-galaxy-calculator.git\n  $ cd merchant-galaxy-calculator\n  $ npm install\n  $ node start\n  "], _b.raw = [" sh\n  $ git clone https://github.com/dennisjade/merchant-galaxy-calculator.git\n  $ cd merchant-galaxy-calculator\n  $ npm install\n  $ node start\n  "], (_c = [""], _c.raw = [""], Steps(_c))(_b))(_a)), (_d = [""], _d.raw = [""], (_e = ["sh\n  $ npm test\n  "], _e.raw = ["sh\n  $ npm test\n  "], (_f = [""], _f.raw = [""], Test(_f))(_e))(_d)), Module, Diagram, ![Routing](docs / images / router.png, "Routes"), ![Compute, API](docs / images / compute - api.png, "Compute API"), ![Input, API](docs / images / input - api.png, "Input API"), APIs
        | Method | Route | Params | Description |
        | --, --, -- -  | --, --, -- -  | --, --, --, -- | --, --, --, --, --, -- -  |
        | GET | /api/input | NONE | Returns, the, cache, data, object |
        | POST | /api/input | input( | Add, value, assignment, to, the, cache, obj |
        | DELETE | /api/input | NONE | Clear, the, cache, obj |
        | GET | /api/compute | question( | Computes, and, returns, the, value)))));
var _c, _b, _a, _f, _e, _d;
